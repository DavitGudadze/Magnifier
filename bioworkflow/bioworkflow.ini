[uwsgi]
module = wsgi:app
master = true
processes = 1
socket = /home/u295178/magnifier/bioworkflow/bioworkflow.sock
http-socket = 127.0.0.1:5001
chmod-socket = 777
# Internal HTTP socket — lets the frontend reach the backend directly on the server
# without going through the public internet or the professor's reverse proxy.
# Only accessible on the server itself (127.0.0.1).
vacuum = true
die-on-term = true
logto = /home/u295178/magnifier/bioworkflow/bioworkflow.log
